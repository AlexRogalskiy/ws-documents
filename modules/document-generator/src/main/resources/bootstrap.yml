# SensibleMetrics Document Generator WebService Bootstrap configuration
---
spring:
  application:
    name: "Document-Generator-WebService"
  cloud:
    kubernetes:
      enabled: false
      discovery:
        enabled: true
      config:
        enabled: true
        sources:
          - name: common-config
          - name: ${spring.application.name}
      secrets:
        enabled: false
        name: ${spring.application.name}
---
spring:
  profiles: compose
  application:
    name: "Document-Generator-WebService"
  cloud:
    kubernetes:
      discovery:
        enabled: true
      config:
        enabled: true
        sources:
          - name: common-config
          - name: ${spring.application.name}
      secrets:
        enabled: true
        name: ${spring.application.name}
