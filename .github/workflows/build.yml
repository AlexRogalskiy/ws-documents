name: Document-Generator-Web-Service
on: [push]

jobs:
  java_8:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [ '8' ]
        architecture: [ 'x64', 'arm' ]
        os: [ubuntu-latest, windows-latest, macOS-latest]
    name: Build with JDK ${{ matrix.java }} on ${{ matrix.architecture }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
          java-package: jdk
          architecture: ${{ matrix.architecture }}
      - name: Build with Maven
        run: mvn -Dfile.encoding=UTF-8 --lax-checksums -DskipTests=true clean package -Pjava_8,test-jar,xsd
  java_11:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [ '11', '12', '13' ]
        architecture: [ 'x64', 'arm' ]
        os: [ubuntu-latest, windows-latest, macOS-latest]
    name: Build with JDK ${{ matrix.java }} on ${{ matrix.architecture }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
          java-package: jdk
          architecture: ${{ matrix.architecture }}
      - name: Build with Maven
        run: mvn -Dfile.encoding=UTF-8 --lax-checksums -DskipTests=true clean package -Pjava_11,test-jar,xsd
